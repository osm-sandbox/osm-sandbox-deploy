charts:
  - name: osm-sandbox
    imagePrefix: ghcr.io/osmus/
    repo:
      git: osmus/osm-sandbox-deploy
      published: https://osmus.github.io/osm-sandbox-deploy
      branch: gh-pages
    images:
      osm-sandbox-web:
        valuesPath: osm-seed.web.image
      sandbox-populate-apidb:
        valuesPath: osm-seed.populateApidb.image
